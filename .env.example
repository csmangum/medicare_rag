# Optional: override data directory (default: repo root / data)
# DATA_DIR=/path/to/data

# ICD-10-CM tabular data (XML) from CMS — update the year as needed
# ICD10_CM_ZIP_URL=https://www.cms.gov/files/zip/2025-code-tables-tabular-and-index.zip

# Phase 3: local embeddings (sentence-transformers); no API key needed
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# Changing the model changes embedding dimension; a new Chroma collection or re-index may be needed.

# Phase 4: local LLM (Hugging Face pipeline); no API key needed
# LOCAL_LLM_MODEL=TinyLlama/TinyLlama-1.1B-Chat-v1.0
# LOCAL_LLM_DEVICE=auto
# LOCAL_LLM_MAX_NEW_TOKENS=512
# LOCAL_LLM_REPETITION_PENALTY=1.05
# Invalid numeric values for the above fall back to defaults (with a warning).
# Use CUDA_VISIBLE_DEVICES to force CPU (e.g. "") or select a GPU (e.g. 0).

# Download
# DOWNLOAD_TIMEOUT=60

# MCD CSV ingestion — max field size in bytes for large LCD/NCD policy text
# CSV_FIELD_SIZE_LIMIT=10485760

# Chunking — standard (IOM and general policy documents)
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=200

# Chunking — LCD/MCD-specific (larger chunks preserve more policy context)
# LCD_CHUNK_SIZE=1500
# LCD_CHUNK_OVERLAP=300

# LCD retrieval — higher k for coverage-determination queries
# LCD_RETRIEVAL_K=12

# Topic clustering and summarization (generates anchor chunks for consistency)
# ENABLE_TOPIC_SUMMARIES=1
# MAX_DOC_SUMMARY_SENTENCES=8
# MAX_TOPIC_SUMMARY_SENTENCES=10
# MIN_TOPIC_CLUSTER_CHUNKS=2
# MIN_DOC_TEXT_LENGTH_FOR_SUMMARY=200

# Hybrid retrieval — semantic + BM25 fusion weights (requires rank-bm25)
# HYBRID_SEMANTIC_WEIGHT=0.6
# HYBRID_KEYWORD_WEIGHT=0.4
# RRF_K=60

# Cross-source diversification
# CROSS_SOURCE_MIN_PER_SOURCE=2
# MAX_QUERY_VARIANTS=6

# Chroma batch sizes
# CHROMA_UPSERT_BATCH_SIZE=5000
# GET_META_BATCH_SIZE=500
